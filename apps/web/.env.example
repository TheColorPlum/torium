# Torium Web App Environment Variables
# Copy to .env.local for local development

# API Base URL - points to Cloudflare Worker
# For local dev with worker running locally: http://localhost:8787/api/v1
# For production (set in Vercel): https://torium.app/api/v1
NEXT_PUBLIC_API_URL=https://torium.app/api/v1
